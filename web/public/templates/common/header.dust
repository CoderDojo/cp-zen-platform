{>"common/cd-menu" /}
<header id="header" ng-controller="login">
    <div class="container">
        {>"common/content-header" /}
            <div class="col-lg-2 cd-user-name-container">
                <a href=""><span class="cd-logout" ng-click="logout()">Logout</span></a>
                <span class="cd-user-name">{{ user.name }} |</span>
            </div>
        </div>
</header>
