<select ng-controller="languageController"
    name="language"
    class="language-picker"
    ng-model="selectedLanguage"
    ng-change="updateLocale()"
    ng-options="language.code  as language.name + (' ('+ language.country +') ') for language in languages">
</select>
